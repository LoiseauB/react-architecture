import { Box, Typography } from "@mui/material";

export const BattleFieldSection: React.FC<object> = () => {
  return (
    <Box>
      <Typography variant="h5">Choose your Battlefield 🪖</Typography>
    </Box>
  );
};
